[
  /*
   * Disable the default bindings as they're handled below
   */
  {
    "context": "Editor",
    "bindings": {
      "ctrl-k v": null // disable side preview for markdown
    }
  },
  {
    "context": "Workspace",
    "bindings": {
      "ctrl-shift-p": null, // Open with : instead!
      "ctrl-alt-b": null, // Open with space e
      "ctrl-shift-f": null, // Open with space f g
      "ctrl-tab": null, // Open with space space
      "ctrl-shift-tab": null // No binding for this
    }
  },
  {
    "context": "Editor",
    "bindings": {
      "ctrl-shift-v": "editor::Paste"
    }
  },
  {
    "context": "VimControl && vim_mode == normal",
    "bindings": {
      "space f f": "file_finder::Toggle",
      "space f m": "editor::Format",
      "space e": "project_panel::ToggleFocus",
      "shift-y": ["workspace::SendKeystrokes", "y $"],
      "space p p": "markdown::OpenPreview",
      "space p s": "markdown::OpenPreviewToTheSide",
      "space f g": "pane::DeploySearch",
      "space space": "tab_switcher::Toggle",
      "space b n": "pane::ActivateNextItem",
      "space b p": "pane::ActivatePreviousItem"
    }
  },
  {
    "context": "vim_operator == y",
    "bindings": {
      "-": ["workspace::SendKeystrokes", "^ y $"]
    }
  },
  {
    "context": "VimControl && vim_mode == visual",
    "bindings": {
      "shift-x": "vim::Exchange"
    }
  },
  {
    "context": "VimControl && vim_mode == normal || vim_mode == visual",
    "bindings": {
      "space a": "editor::SelectAll",
      "alt-k": "editor::MoveLineUp",
      "alt-j": "editor::MoveLineDown",
      "s": "vim::PushSneak", // <leader>' → Sneak forward
      "alt-s": "vim::PushSneakBackward" // <leader>" → Sneak backward
    }
  },
  {
    "context": "ProjectPanel && not_editing",
    "bindings": {
      ".": "project_panel::ToggleHideGitIgnore",
      "q": "workspace::ToggleRightDock",
      "space e": "workspace::ToggleRightDock",
      "h": null,
      "z": "project_panel::CollapseSelectedEntry"
    }
  }
]
